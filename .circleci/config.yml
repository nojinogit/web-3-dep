version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: Start all services declared in docker-compose.yml
          command: docker-compose up -d
